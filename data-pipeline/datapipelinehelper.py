def process_with_bounding_boxes(images_path, bounding_boxes_path, backgrounds_path, output_width=640, output_height=640):
    return 

def process_without_bounding_boxes(images_path, bounding_boxes_path, backgrounds_path, output_width=640, output_height=640):
    return 

def superimpose_image(image, background):
    return 